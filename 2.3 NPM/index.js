//var generateName = require("sillyname");
//the code below is the import ver of it called ecma

import generateName from "sillyname";
var sillyName = generateName();

console.log(`My name is ${sillyName}.`);
